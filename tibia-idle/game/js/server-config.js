/* Configuração padrão: build estático continua no modo local.
 * O test server sobrescreve este arquivo dinamicamente na mesma URL. */
window.GLOBAL_IDLE_SERVER_CONFIG = {
  online: false,
  testServer: false,
  apiUrl: "",
};

{
  "name": "global-idle-alpha",
  "version": "1.0.0",
  "private": true,
  "description": "Pacote de deploy do Global-Idle (jogo + API) para SaveInCloud/Jelastic. Na branch cursor/alpha-test-deploy isto é a raiz do repo; no monorepo é a pasta tibia-idle/.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server.js",
    "start:test": "node server.js",
    "seed": "node server/seed.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.6.1",
    "mysql2": "^3.14.3",
    "nodemailer": "^6.9.16"
  }
}
